# cbar launcher core

This crate contains the toolkit-independent configuration, model, navigation,
placement, visibility, and usage logic for cbar's resident matrix launcher.
It performs no drawing and launches no processes.

The code originated in the MIT-licensed launcher recorded in `ORIGIN.md` and
is maintained here as part of the single cbar component.
